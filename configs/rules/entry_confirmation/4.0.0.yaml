rule_id: marketbot.entry_confirmation
version: 4.0.0
status: active
engines:
  swing: 3.0.0
  intraday: 4.0.0
  entry_watcher: 4.0.0
behavior:
  anchored_vwap_gate: true
  persistence_required: true
  minimum_momentum_percent: 0.15
  minimum_risk_percent: 0.25
  minimum_atr_risk_multiple: 0.50
  reward_risk_ratio: 1.5
  maximum_trigger_extension_atr: 0.50
  maximum_ema20_extension_atr: 2.00
  strong_confirmation_required: true
  five_minute_higher_low_required: true
  fresh_reconfirmation_delay_minutes: 3
  trigger_rearm_cooldown_minutes: 30
measurement:
  dimensions:
    - engine_version
    - entry_confirmation_rule_version
    - entry_timing
  outcomes:
    - first_level_hit
    - mfe_percent
    - mae_percent
    - return_15m
    - return_30m
    - return_60m
    - return_close
